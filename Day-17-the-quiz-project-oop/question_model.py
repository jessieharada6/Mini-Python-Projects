class Question:
    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer


# for index, q in sorted(question_data):
#     q_name  = "question_" + index
#     q_name = Question(q["text"], q["answer"])
#     print(q_name)
